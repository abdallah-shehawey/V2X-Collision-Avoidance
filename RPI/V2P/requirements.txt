# Dependencies for RPI/V2P/V2P.py.
# picamera2 is normally installed via apt (python3-picamera2) rather than pip on
# Raspberry Pi OS, since it depends on system libcamera bindings - listed here for
# documentation completeness, not necessarily pip-installable as-is on every setup.
# No lockfile previously existed in this repo (see AI_TECHNICAL_AUDIT.md, Finding DEP1).
opencv-python>=4.8
numpy>=1.24
onnxruntime>=1.16
picamera2>=0.3
