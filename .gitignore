V2V-STM32/Debug/
.obsidian/
.vscode/
.metadata/
.cproject
.project
.settings/
Debug/
__pycache__/
.claude/

# Large ML model binaries — keep out of git (see RPI/V2P/README for where to get them).
# The real models live on the Pi; a stray checkout/pull must not overwrite them.
*.onnx
*.pt
*.pt.zip