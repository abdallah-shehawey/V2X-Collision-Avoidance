# Dependencies for Traffic_Light/ (distance.py, Intelligent_Gateway.py, Traffic_light_GUI.py).
# No lockfile previously existed in this repo (see AI_TECHNICAL_AUDIT.md, Finding DEP1) -
# these are functional minimums, not exact versions verified against a known-working
# environment. Once you have one working, replace this with `pip freeze > requirements.txt`.
opencv-python>=4.8
ultralytics>=8.0
easyocr>=1.7
paho-mqtt>=2.0
